title: Accessible Images
description: >-
  A course on how to make images in web apges accessible to people with
  disabilities.
template:
  repo: accessible-images-template
  name: accessible-images
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
